# Server Configuration
server:
  port: 8080
  servlet:
    context-path: /

# Database Configuration (same as application.properties)
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/timetable?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: rekshin#01
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
    properties:
      hibernate:
        format_sql: true

# Logging Configuration
logging:
  level:
    root: INFO
    com.example.timetable: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
